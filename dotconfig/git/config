[user]
	name = "e.j. (they/them)"
	email = git@ejs.sh

[github]
	user = ejsdotsh

[core]
	autocrlf = input
	editor = nvim

[color]
	ui = auto

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green

[color "diff"]
	meta = yellow bold
	frag = magenta bold # line info
	old = red # deletions
	new = green # additions

[color "status"]
	added = yellow
	changed = green
	untracked = cyan

[init]
	defaultBranch = main

[pull]
	rebase = false
